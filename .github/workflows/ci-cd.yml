name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/fastapi_app:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Decode SSH Key
      run: |
        echo "${{ secrets.VPS_SSH_KEY }}" | base64 --decode > /tmp/id_rsa
        chmod 600 /tmp/id_rsa

    - name: SSH into server and deploy
      uses: appleboy/ssh-action@v0.1.4
      with:
        host: ${{ secrets.VPS_HOST }}
        username: root
        key_path: /tmp/id_rsa
        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/fastapi_app:latest
          docker stop fastapi_container || true
          docker rm fastapi_container || true
          docker run -d --name fastapi_container -p 80:80 ${{ secrets.DOCKER_USERNAME }}/fastapi_app:latest